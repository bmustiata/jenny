stage = { name, code ->
    _runSectionWithId("stage") { id ->
        _log.message("> ========================================================================")
        _log.message("> = Stage: " + name)
        _log.message("> ========================================================================")
        code()
    }
}
