stage('child') {
    node {
        sh """
            echo "hello from child"
            pwd
        """
    }
}

stage('child Deploy') {
    node {
        sh """
            echo "wutinston"
        """
    }
}

